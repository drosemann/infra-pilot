name: CI - Integration Service (Python)

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'services/integration-service/**'
      - '.github/workflows/ci-integration.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'services/integration-service/**'

env:
  SERVICE_PATH: services/integration-service
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/integration-service

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint & Format Check

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black isort

      - name: Run flake8
        run: |
          flake8 ${{ env.SERVICE_PATH }}/src --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 ${{ env.SERVICE_PATH }}/src --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Check black formatting
        run: black --check ${{ env.SERVICE_PATH }}

      - name: Check import sorting
        run: isort --check-only ${{ env.SERVICE_PATH }}

  test:
    runs-on: ubuntu-latest
    name: Unit Tests

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r ${{ env.SERVICE_PATH }}/requirements.txt
          pip install pytest pytest-asyncio pytest-cov

      - name: Run pytest
        run: |
          cd ${{ env.SERVICE_PATH }}
          pytest -q --cov=src --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./${{ env.SERVICE_PATH }}/coverage.xml
          flags: integration-service
          name: integration-service

  security:
    runs-on: ubuntu-latest
    name: Security Checks

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install security tools
        run: |
          pip install bandit safety

      - name: Run Bandit security check
        run: bandit -r ${{ env.SERVICE_PATH }}/src -f json -o bandit-report.json || true

      - name: Check dependencies for vulnerabilities
        run: |
          pip install -r ${{ env.SERVICE_PATH }}/requirements.txt
          safety check --json || true

  build:
    needs: [lint, test, security]
    runs-on: ubuntu-latest
    name: Build Service Image
    if: github.event_name == 'push'

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ${{ env.SERVICE_PATH }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

[pytest]
testpaths = tests
markers =
  unit: Unit tests
  integration: Integration tests
  e2e: End-to-end tests using mocked providers
  smoke: Fast import and health checks
addopts = --cov=vps_manager --cov-report=xml:coverage.xml --cov-report=term-missing --cov-fail-under=35
